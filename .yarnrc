# Keep one canonical dependency tree in the repository root.
# .work is reserved for mutable caches/build/log/state, not node_modules.
--modules-folder "node_modules"
--cache-folder ".work/cache/yarn"
